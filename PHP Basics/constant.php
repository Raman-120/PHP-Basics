<?php

    define("info", "This is a simple demonstration of constants in php", false);
    // if case insesitive is false then it demands the same name in echo else will throw
    // errors but if we keep it true then it will just ignore the case and print what is in the constant
    echo info;

?>